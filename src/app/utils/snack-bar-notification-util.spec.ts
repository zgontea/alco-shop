import { SnackBarNotificationUtil } from './snack-bar-notification-util';

describe('SnackBarNotificationUtil', () => {
  it('should create an instance', () => {
    expect(new SnackBarNotificationUtil()).toBeTruthy();
  });
});
